# Copy this to .env and adjust if your backend ports differ from the defaults.
#
# Post-migration: one auth server (Java) issues tokens; one business
# backend (Python/FastAPI) serves every business module behind that token.
VITE_AUTH_SERVICE_URL=http://localhost:8080
VITE_BUSINESS_SERVICE_URL=http://localhost:8000
