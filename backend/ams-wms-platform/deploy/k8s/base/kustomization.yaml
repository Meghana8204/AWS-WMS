apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ams-wms
resources:
  - namespace.yaml
  - configmap.yaml
  - auth-service-deployment.yaml
  - business-service-deployment.yaml
  - ingress.yaml
  # secrets.example.yaml intentionally NOT included - apply your own
  # generated secrets separately, never commit real ones.
