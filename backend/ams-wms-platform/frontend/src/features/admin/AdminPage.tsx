/**
 * Placeholder. This becomes the master data / RBAC console once platform's
 * identity and masterdata modules exist.
 */
export default function AdminPage() {
  return (
    <section className="page">
      <h1>Admin console</h1>
      <p className="page-subtitle">
        Placeholder. Wire this up to platform's identity and master-data
        endpoints as those modules are built out.
      </p>
    </section>
  )
}
