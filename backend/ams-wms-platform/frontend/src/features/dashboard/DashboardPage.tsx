/**
 * Placeholder. This becomes its own route-based experience (and later its
 * own bundle - see /docs/ARCHITECTURE.md section 5.4) for KPIs and
 * approvals once the platform module's approval workflow exists.
 */
export default function DashboardPage() {
  return (
    <section className="page">
      <h1>Operations dashboard</h1>
      <p className="page-subtitle">
        Placeholder. Wire this up to platform's approval and notification
        endpoints as those modules are built out.
      </p>
    </section>
  )
}
